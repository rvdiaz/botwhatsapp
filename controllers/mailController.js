const nodemailer=require('nodemailer');

/* const yourEmail = "valdesdiazroberto@gmail.com";
const yourPass = "robertovaldes1994";
const mailHost = "localhost";
const mailPort = 4001;
const senderEmail = "rvdiaz@gmail.com"

const sendMail = (to, subject, htmlContent) => {
    let transporter = nodemailer.createTransport({
      host: mailHost,
      port: mailPort,
      secure: false, // use SSL - TLS
      auth: {
        user: yourEmail,
        pass: yourPass,
      },
    });
    let mailOptions = {
        from: senderEmail,
        to: to,
        subject: subject,
        html: htmlContent,
      };
      return transporter.sendMail(mailOptions); // promise
    } */

